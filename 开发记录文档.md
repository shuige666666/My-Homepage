### 初始方案

#### 最初始的需求

那我们先从简单的开始，主页先只实现动漫相关的内容，然后暂时不搞复杂的动效什么的，但是网页的审美和设计一定要高级，你可以借鉴一些有关前端的skills，然后采用grill-me进行开发

bangumi的api文档：https://bangumi.github.io/api/

我的username是：860550



##### 技术栈

#### 由于我打算做的这个网页偏静态，因此技术栈采用：

Astro + TypeScript + CSS

假如后续还需要加上一些动效或者其它的东西我会建议你用：

```
Astro + TypeScript + CSS 
局部交互：React / Svelte / 原生 JS 
动效：GSAP / Motion One / Framer Motion / Three.js 
部署：Cloudflare Pages / Vercel / GitHub Pages
```

> Astro 的好处是，它可以让大部分页面保持静态、加载快、SEO 友好，同时在需要动效的地方再“按需加交互”。比如首页首屏有一个动态背景、动漫卡片有悬浮动画、时间线滚动出现、角色收藏墙有筛选和过渡，这些都不需要把整站变成 React 应用。



#### 大致项目结构

核心思路是：**页面负责组合、组件负责展示、内容集合负责可重复的展示数据、服务层负责外部 API、样式按职责拆分。** 第一阶段保持纯静态，未来接入 Bangumi、博客、复杂动效时不用重构整体结构。

##### **推荐项目结构**

```text
my-homepage/
├─ public/
│  ├─ favicon.svg
│  ├─ fonts/
│  ├─ images/
│  │  ├─ profile/
│  │  ├─ anime/
│  │  └─ backgrounds/
│  └─ robots.txt
│
├─ src/
│  ├─ assets/
│  │  └─ images/
│  │
│  ├─ components/
│  │  ├─ common/
│  │  │  ├─ SiteHeader.astro
│  │  │  ├─ SiteFooter.astro
│  │  │  └─ SectionTitle.astro
│  │  ├─ home/
│  │  │  ├─ HeroSection.astro
│  │  │  ├─ AboutSection.astro
│  │  │  └─ FeaturedAnime.astro
│  │  ├─ anime/
│  │  │  ├─ AnimeCard.astro
│  │  │  ├─ AnimeGrid.astro
│  │  │  └─ AnimeStatus.astro
│  │  └─ interactive/
│  │     └─ ThemeToggle.ts
│  │
│  ├─ content/
│  │  ├─ anime/
│  │  │  ├─ bocchi-the-rock.md
│  │  │  └─ steins-gate.md
│  │  ├─ projects/
│  │  └─ posts/
│  │
│  ├─ data/
│  │  ├─ profile.ts
│  │  ├─ social-links.ts
│  │  └─ site-config.ts
│  │
│  ├─ layouts/
│  │  ├─ BaseLayout.astro
│  │  └─ ContentLayout.astro
│  │
│  ├─ pages/
│  │  ├─ index.astro
│  │  ├─ anime/
│  │  │  ├─ index.astro
│  │  │  └─ [id].astro
│  │  ├─ projects.astro
│  │  ├─ about.astro
│  │  └─ 404.astro
│  │
│  ├─ services/
│  │  └─ bangumi.ts
│  │
│  ├─ styles/
│  │  ├─ tokens.css
│  │  ├─ reset.css
│  │  ├─ global.css
│  │  └─ utilities.css
│  │
│  ├─ types/
│  │  ├─ bangumi.ts
│  │  └─ site.ts
│  │
│  ├─ utils/
│  │  ├─ date.ts
│  │  └─ image.ts
│  │
│  └─ content.config.ts
│
├─ scripts/
│  └─ sync-bangumi.ts
│
├─ astro.config.ts
├─ tsconfig.json
├─ package.json
└─ README.md
```

##### **各部分如何分工**

`pages` 目录决定网站路由。首页尽量只负责组合不同区块，不在里面堆积大量 HTML 和数据处理代码。

```astro
<BaseLayout>
  <HeroSection />
  <AboutSection />
  <FeaturedAnime />
</BaseLayout>
```

`components` 存放展示组件，并按照业务区域分类。只有真正会跨页面复用的组件才放进 `common`，避免一开始建立过度抽象的组件库。

`content` 用于管理动漫、文章、项目等“同类型、多条目”的内容。每部动漫可以对应一个 Markdown 文件，里面保存评分、封面、标签和个人感想。Astro 当前推荐使用 `src/content.config.ts` 定义 Content Collections，并通过 schema 检查内容格式。

例如：

```md
---
title: 孤独摇滚！
cover: /images/anime/bocchi-the-rock.webp
score: 9
status: watched
tags:
  - 音乐
  - 日常
featured: true
---

非常喜欢作品对社恐体验的夸张表达……
```

`data` 则用于不需要独立页面的简单数据，例如个人介绍、社交链接和网站配置。区分方式可以理解为：

- 多条结构一致、可能生成详情页的数据放入 `content`。
- 少量全局配置和简单列表放入 `data`。
- 外部平台返回的数据通过 `services` 获取。

##### **外部 API 的组织方式**

Bangumi 请求逻辑不要直接写进页面组件，而是集中放在：

```text
src/services/bangumi.ts
src/types/bangumi.ts
```

第一阶段可以在构建时获取公开数据。为了避免构建依赖第三方接口，也可以运行 `scripts/sync-bangumi.ts`，将账号数据同步为本地 JSON，再由 Astro 构建页面。

未来需要隐藏 token 或实时请求时，可以增加：

```text
src/pages/api/bangumi.ts
```

不过这会要求部署平台支持 Astro 服务端适配器；当前阶段无需添加。

##### **CSS 的组织方式**

建议使用原生 CSS，不急着引入 Tailwind。全局样式只负责设计变量和基础规则：

```css
/* tokens.css */
:root {
  --color-background: #0d0e12;
  --color-surface: #181a21;
  --color-text: #f4f4f5;
  --color-accent: #ef476f;

  --content-width: 1120px;
  --space-section: 6rem;
}
```

每个 Astro 组件的专属样式直接写在组件内的 `<style>` 中，Astro 会自动限制作用域。以后加入动效时，可以新增 `src/scripts/animations/`，不需要提前创建。



#### 设计方案

##### **页面定位**

首版仅制作“我的动漫偏好”单页。页面以展示个人动漫审美为主，Bangumi 数据为辅，整体参考设计图中的清透、安静、自然风格，不制作复杂数据面板和独立动漫详情页。

##### **技术方案**

采用：

```text
Astro + TypeScript + 原生 CSS
```

页面保持静态生成。同步脚本从 Bangumi API 获取数据并保存为本地 JSON，Astro 构建时读取本地数据。首版动效主要使用 CSS 和少量原生 TypeScript，不引入 GSAP、React 等额外依赖。

部署后通过 GitHub Actions 每天同步一次 Bangumi 数据，同时支持手动触发。同步失败时保留旧数据，避免影响现有页面。

##### **页面结构**

顶部导航精简为：

```text
我的动漫偏好 | 看过的 | 记录
```

“我的动漫偏好”作为品牌标题，点击返回页面顶部；“看过的”和“记录”使用外链图标，并在新标签页打开：

```text
看过的：https://bangumi.tv/anime/list/860550/collect
记录：https://bangumi.tv/user/860550/timeline
```

正文包含以下区域：

1. **个人概览**

展示本地头像、Bangumi 用户名、本地占位自我介绍、Bangumi 个人主页入口，以及“看过、在看、想看”三个数量。

用户名由同步脚本读取，失败时回退为 `860550`。头像由本地资源提供，完成后替换指定文件即可。

2. **最喜欢的动漫**

从用户 `860550` 的动画收藏中筛选已评分条目，按个人评分降序排列，取前十。同分时按 Bangumi 社区评分降序排列。

剧场版、OVA、特别篇、续作季度全部参与排名。点击封面或标题会跳转至对应 Bangumi 条目，不在本站制作详情页。

同时保留未来切换为手动精选模式的能力，并支持个人短句的全局开关。首版关闭短句展示，也不预留空白区域。

3. **最近看过**

自动展示最近更新过的 5 部“看过”作品，并显示封面、名称、个人评分和记录日期。

4. **最近评分与短评**

展示最近 2～4 条有个人评分的记录。存在短评时展示短评；没有短评时只显示作品名称、个人评分与日期，不渲染空评论区域。

5. **页脚**

放置简单版权信息与 Bangumi 数据来源说明。

首版不制作偏好标签、复杂图表、评分趋势、搜索、本站“看过的”列表和本站活动记录页。

##### **视觉与交互**

使用颜色方案：

```text
#FFF471  黄色强调色
#DFF1F4  浅蓝背景色
#8FBCD6  主蓝色
#93BCA3  柔和绿色
#D9E366  黄绿色辅助色
```

视觉延续参考图的清透卡片、淡色背景、柔和自然装饰和轻阴影，但会控制卡片嵌套与装饰数量，保证页面具有个人气质而不是普通仪表盘。

首版采用轻量动效：页面区域依次淡入、封面悬停抬升与图片放大、最喜欢的动漫横向拖动和按钮切换、背景装饰轻微视差，并支持系统的“减少动态效果”设置。

##### **数据与扩展性**

Bangumi 数据会由同步脚本生成本地 JSON。页面配置与远程数据分离，方便以后修改自我介绍、头像、短句开关和精选规则。

头像预计放置在：

```text
public/images/profile/avatar.webp
```

后续可以继续扩展本站“看过的”排序页面、完整记录页、偏好标签、更多动效，以及整个个人主页的全局首页。



#### 部署方案

```text
Private GitHub 仓库
        ↓
GitHub Actions 每日同步 Bangumi 数据
        ↓
提交更新后的 bangumi.json
        ↓
Cloudflare Pages 自动构建并发布
```

##### **第一步：创建 GitHub 私有仓库**

登录 GitHub，访问 [创建新仓库](https://github.com/new)，填写：

```text
Repository name: my-anime-homepage
Visibility: Private
```

不要勾选初始化 `README`、`.gitignore` 或 License，因为本地项目已经包含这些文件。

创建完成后，复制仓库 HTTPS 地址：

```text
https://github.com/你的用户名/my-anime-homepage.git
```

##### **第二步：提交并推送本地项目**

在项目目录中执行：

```powershell
git add .
git commit -m "feat: create anime preferences page"
git branch -M main
git remote add origin https://github.com/你的用户名/my-anime-homepage.git
git push -u origin main
```

如果以前添加过 `origin`，可以先检查：

```powershell
git remote -v
```

然后使用：

```powershell
git remote set-url origin https://github.com/你的用户名/my-anime-homepage.git
```

推送之前建议确认没有把密钥、Token 或代理配置提交进去。当前同步脚本不需要 Bangumi Token，`7890` 代理也只通过本地环境变量使用，不会提交到 GitHub。

##### **第三步：确认 GitHub Actions 可以运行**

推送完成后，进入仓库的 `Actions` 页面。第一次使用时，GitHub 可能要求点击启用 Actions。

找到：

```text
Sync Bangumi data
```

点击：

```text
Run workflow → Run workflow
```

它会执行：

```text
npm ci
npm run sync:bangumi
npm run build
提交更新后的 src/data/bangumi.json
```

当前工作流每天北京时间 **02:20** 自动运行一次。GitHub 云端通常能够直接访问 Bangumi，不需要配置本地代理。

私有仓库可以运行 Actions，但会消耗账户的 Actions 免费额度。GitHub Free 当前每月包含 2,000 分钟；这个项目每天运行一次，通常只会消耗几十到一百多分钟，远低于额度。

##### **第四步：连接 Cloudflare Pages**

登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)，依次进入：

```text
Workers & Pages
→ Create application
→ Pages
→ Import an existing Git repository
→ Connect GitHub
```

GitHub 会询问允许 Cloudflare 访问哪些仓库。为了权限最小化，选择：

```text
Only select repositories
→ my-anime-homepage
```

即使仓库是 Private，Cloudflare 获得授权后仍能读取并部署它。

##### **第五步：配置构建参数**

选择仓库后，填写：

```text
Project name: my-anime-homepage
Production branch: main
Framework preset: Astro
Build command: npm run build
Build output directory: dist
```

建议在环境变量中增加：

```text
NODE_VERSION = 20.19.0
```

然后点击 `Save and Deploy`。

Cloudflare 会运行安装与构建，成功后提供地址：

```text
https://my-anime-homepage.pages.dev
```

##### **自动更新是怎样触发的**

以后每天的完整流程是：

```text
GitHub Actions 定时运行
→ 从 Bangumi 获取最新记录
→ bangumi.json 发生变化
→ Actions 自动提交并推送到 main
→ Cloudflare 检测到 main 出现新提交
→ 自动运行 npm run build
→ 新页面发布
```

如果当天 Bangumi 数据没有变化，工作流不会产生新提交，Cloudflare 也不会重复部署。

你修改页面代码并推送到 `main` 后，Cloudflare 同样会立即重新部署。Cloudflare Pages 也会为 Pull Request 创建独立的预览地址。

需要注意：Private 仓库保护的是源代码，`src/data/bangumi.json` 中的数据最终会进入公开网页，因此不要把私密 Token 或不希望公开的数据写入其中。

参考资料：[Cloudflare Pages Git 集成](https://developers.cloudflare.com/pages/get-started/git-integration/) · [Cloudflare Astro 部署指南](https://developers.cloudflare.com/pages/framework-guides/deploy-an-astro-site/) · [GitHub Actions 私有仓库计费](https://docs.github.com/en/billing/concepts/product-billing/github-actions)
